int i, j;
j = print(read(i));
if (i < 100)
{
	j = 100-j;
	while (i<100)
	{
		boolean b;
		read(b);
		string checker =
		if (b)
		{
			return "you said true?";
		}
		else
		{
			return "you said false?";
		};
		print(checker);
		i = i+1;
		j = j*j;
	};
}
else
{
	j = j-100;
	while (i>100)
	{
		char c;
		read(c);
		string yourChar =
		if (c == 'r')
		{
			return "b-e-a-utiful!";
		}
		else
		{
			return "Your character is not so good, visit a psychologist please.";
		};
		print(yourChar);
		i = i-1;
		j = j*j;
	};
};
print("The difference between your number and zero that many times plus one to its own power: ");
print(j);
print("Next time take a number closer to 100.");