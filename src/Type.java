public enum Type {
	/**
	 * Deze typen worden gebruikt voor de type-checking in de slfChecker en voor het genereren van de juiste type-afhankelijke instructies in de slfGenerator.
	 */
	INTEGER, CHARACTER, BOOLEAN, STRING, VOID, ERROR;
}